const is = require('is')
const isEmptyObject = (value)=>{
    if(is.object(value) && !is.){
    }
}

        // "lodash.isempty": "^4.4.0",
        // "lodash.isfunction": "^3.0.9",
        // "lodash.isplainobject": "^4.0.6",
        // "lodash.transform": "^4.6.0"


const isEmptyString = (value)=>{
    if(!is.nil(value)){

    }
}
const isEmptyArray = (value)=>{

}
