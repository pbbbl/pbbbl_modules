{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gitpush":"lerna bootstrap && lerna link && git add . && git commit -m \"prepublish\" && git push",
    "ver": "lerna version --yes --conventional-commits",
    "prerelease":"lerna bootstrap && lerna link && git add . && git commit -m \"prepublish\" && git push",
    "release:patch": "yarn on-release && lerna exec \"yarn publish --access public --patch\"",
    "release:minor": "yarn on-release && lerna exec \"yarn publish --access public --minor\"",
    "release:major": "yarn on-release && lerna exec \"yarn publish --access public --major\"",
    "release": "yarn on-release && lerna exec \"yarn publish --access public --patch\""
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
