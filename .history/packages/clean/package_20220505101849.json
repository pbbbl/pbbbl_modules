{
    "version": "0.13.0",
    "name": "@pbbbl/clean",
    "private": false,
    "public": true,
    "main": "dist/index.js",
    "license": "MIT",
    "description": "Fork of clean-deep by nonofgs with option to remove functions",
    "scripts": {
        "copy-src-to-dist": "npx copyfiles -u 1 ./src/**/*.* ./dist",
        "clean-dist": "rimraf ./dist/*",
        "build": "npm run clean-dist && npm run copy-src-to-dist",
        "on-release": "npm run build",
        "release": "npm run build && npm publish --access public"
    },
    "dependencies": {
    },
    "devDependencies": {
        "copyfiles": "^2.4.1",
        "rimraf": "^3.0.2"
    }
}
